<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Demo Gallery - APS Advertising</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Montserrat:wght@400;500;600;700&display=swap"
        rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-red: #D2042D;
            --accent-red: #FF3366;
            --primary-black: #000000;
            --dark-grey: #0A0A0A;
            --medium-grey: #1A1A1A;
            --light-grey: #333333;
            --off-white: #F5F5F5;
            --pure-white: #FFFFFF;
            --card-bg: rgba(26, 26, 26, 0.95);
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--primary-black);
            color: var(--pure-white);
            line-height: 1.5;
            font-size: 15px;
        }

        @media (max-width: 768px) {
            body {
                font-size: 14px;
            }
        }

        h1,
        h2,
        h3,
        h4 {
            font-family: 'Montserrat', sans-serif;
            font-weight: 600;
        }

        /* ========== HEADER ========== */
        .demo-header {
            background: var(--primary-black);
            padding: 12px 4%;
            border-bottom: 1px solid var(--light-grey);
            position: sticky;
            top: 0;
            z-index: 1000;
            display: flex;
            justify-content: space-between;
            align-items: center;
            min-height: 60px;
        }

        .header-left {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .back-btn {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 36px;
            height: 36px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            color: var(--pure-white);
            text-decoration: none;
            transition: all 0.3s ease;
            border: 1px solid transparent;
        }

        .back-btn:hover {
            background: var(--primary-red);
            border-color: var(--primary-red);
            transform: translateX(-3px);
        }

        .header-logo {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .header-logo-img {
            height: 36px;
            width: 36px;
            object-fit: cover;
            border-radius: 50%;
            border: 2px solid var(--primary-red);
        }

        .header-logo-text {
            font-size: 1.1rem;
            font-weight: 700;
            color: var(--pure-white);
        }

        @media (max-width: 768px) {
            .header-logo-text {
                display: none;
            }
        }

        .header-actions {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        /* ========== HERO SECTION ========== */
        .demo-hero {
            padding: 30px 4% 20px;
            text-align: center;
        }

        .demo-hero h1 {
            font-size: 1.8rem;
            margin-bottom: 8px;
            color: var(--pure-white);
            font-weight: 700;
        }

        .demo-hero p {
            color: rgba(255, 255, 255, 0.8);
            max-width: 600px;
            margin: 0 auto;
            font-size: 0.95rem;
        }

        /* ========== SEARCH SECTION ========== */
        .search-section {
            padding: 0 4% 25px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .search-container {
            background: var(--card-bg);
            border-radius: 12px;
            padding: 20px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
        }

        .search-box {
            display: flex;
            gap: 12px;
            margin-bottom: 20px;
        }

        @media (max-width: 768px) {
            .search-box {
                flex-direction: column;
            }
        }

        .search-input {
            flex: 1;
            padding: 14px 18px;
            border-radius: 10px;
            border: 1px solid var(--light-grey);
            background: rgba(0, 0, 0, 0.4);
            color: var(--pure-white);
            font-family: 'Poppins', sans-serif;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .search-input:focus {
            outline: none;
            border-color: var(--primary-red);
            box-shadow: 0 0 0 3px rgba(210, 4, 45, 0.2);
        }

        .search-btn {
            padding: 14px 28px;
            background: var(--primary-red);
            color: var(--pure-white);
            border: none;
            border-radius: 10px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            font-family: 'Poppins', sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            min-width: 120px;
            /* Clean look */
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .search-btn:hover {
            background: #b00325;
            /* Darker red on hover */
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(210, 4, 45, 0.2);
        }

        .filters-container {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            justify-content: center;
        }

        @media (max-width: 768px) {
            .filters-container {
                justify-content: flex-start;
                overflow-x: auto;
                padding-bottom: 8px;
                -webkit-overflow-scrolling: touch;
            }
        }

        .filter-btn {
            padding: 8px 16px;
            background: rgba(255, 255, 255, 0.08);
            border: 1px solid rgba(255, 255, 255, 0.15);
            border-radius: 20px;
            color: rgba(255, 255, 255, 0.9);
            font-size: 0.9rem;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 6px;
            white-space: nowrap;
        }

        .filter-btn:hover,
        .filter-btn.active {
            background: var(--primary-red);
            color: var(--pure-white);
            border-color: var(--primary-red);
            transform: translateY(-2px);
        }

        /* ========== PORTFOLIO SECTION ========== */
        .portfolio-section {
            padding: 0 4% 40px;
            max-width: 1400px;
            margin: 0 auto;
        }

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 2px solid rgba(255, 255, 255, 0.1);
        }

        .section-header h2 {
            font-size: 1.5rem;
            color: var(--pure-white);
            font-weight: 700;
        }

        .results-count {
            font-size: 0.9rem;
            color: rgba(255, 255, 255, 0.7);
            background: rgba(255, 255, 255, 0.05);
            padding: 6px 12px;
            border-radius: 12px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        /* Portfolio Grid - FIXED for visibility */
        .portfolio-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 25px;
            margin-bottom: 30px;
        }

        @media (max-width: 1024px) {
            .portfolio-grid {
                grid-template-columns: repeat(3, 1fr);
            }
        }

        @media (max-width: 768px) {
            .portfolio-grid {
                grid-template-columns: repeat(2, 1fr);
                gap: 20px;
            }
        }

        @media (max-width: 480px) {
            .portfolio-grid {
                grid-template-columns: 1fr;
                gap: 15px;
            }
        }

        /* Media Cards - FIXED for visibility */
        .media-card {
            background: var(--card-bg);
            border-radius: 16px;
            overflow: hidden;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            border: 1px solid rgba(255, 255, 255, 0.1);
            position: relative;
            height: 100%;
            display: flex;
            flex-direction: column;
        }

        .media-card:hover {
            transform: translateY(-8px);
            border-color: var(--primary-red);
            box-shadow: 0 15px 35px rgba(210, 4, 45, 0.25);
        }

        /* Media Container - FIXED for visibility */
        .media-container {
            position: relative;
            aspect-ratio: 16/9;
            overflow: hidden;
            cursor: pointer;
            background: linear-gradient(135deg, #0a0a0a, #1a1a1a);
        }

        /* Video Thumbnail */
        .video-thumbnail {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.6s ease;
        }

        .media-container:hover .video-thumbnail {
            transform: scale(1.08);
        }

        /* Audio Visual */
        .audio-visual {
            width: 100%;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, rgba(210, 4, 45, 0.1), rgba(0, 0, 0, 0.3));
            position: relative;
        }

        .audio-visual i {
            font-size: 3.5rem;
            color: var(--primary-red);
            opacity: 0.9;
            filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.3));
        }

        /* Play Button Overlay - FIXED for visibility */
        .play-overlay {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.4);
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 1;
            /* Always visible for better UX */
            transition: background 0.3s ease;
            backdrop-filter: blur(0px);
        }

        .media-container:hover .play-overlay {
            background: rgba(0, 0, 0, 0.6);
            backdrop-filter: blur(2px);
        }

        .play-btn {
            width: 60px;
            height: 60px;
            background: var(--primary-red);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.5rem;
            transition: all 0.3s ease;
            border: 3px solid rgba(255, 255, 255, 0.3);
            box-shadow: 0 6px 20px rgba(210, 4, 45, 0.4);
        }

        .media-container:hover .play-btn {
            transform: scale(1.1);
            background: var(--accent-red);
        }

        /* Media Info */
        .media-info {
            padding: 20px;
            flex: 1;
            display: flex;
            flex-direction: column;
        }

        .media-title {
            font-size: 1.2rem;
            font-weight: 700;
            color: var(--pure-white);
            margin-bottom: 10px;
            line-height: 1.3;
        }

        .media-description {
            font-size: 0.9rem;
            color: rgba(255, 255, 255, 0.8);
            margin-bottom: 15px;
            flex: 1;
            line-height: 1.5;
        }

        .media-tags {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            margin-bottom: 15px;
        }

        .tag {
            font-size: 0.75rem;
            padding: 4px 10px;
            background: rgba(210, 4, 45, 0.15);
            color: var(--primary-red);
            border-radius: 12px;
            border: 1px solid rgba(210, 4, 45, 0.3);
        }

        .media-actions {
            display: flex;
            gap: 10px;
        }

        .action-btn {
            flex: 1;
            padding: 10px 16px;
            border-radius: 10px;
            font-weight: 600;
            font-size: 0.9rem;
            text-decoration: none;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            text-align: center;
            cursor: pointer;
        }

        .book-btn {
            background: var(--primary-red);
            color: var(--pure-white);
            border: 2px solid var(--primary-red);
        }

        .book-btn:hover {
            background: var(--accent-red);
            border-color: var(--accent-red);
            transform: translateY(-2px);
        }

        /* Smart Results Notice */
        .smart-notice {
            background: rgba(210, 4, 45, 0.1);
            border-radius: 12px;
            padding: 16px 20px;
            margin-bottom: 25px;
            border: 1px solid rgba(210, 4, 45, 0.2);
            display: flex;
            align-items: center;
            gap: 12px;
            animation: slideIn 0.5s ease;
        }

        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .notice-icon {
            color: var(--primary-red);
            font-size: 1.3rem;
        }

        .notice-text {
            font-size: 0.95rem;
            color: rgba(255, 255, 255, 0.9);
            flex: 1;
        }

        /* No Results State */
        .no-results {
            grid-column: 1 / -1;
            text-align: center;
            padding: 60px 20px;
            background: rgba(255, 255, 255, 0.03);
            border-radius: 16px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .no-results i {
            font-size: 3rem;
            color: var(--primary-red);
            margin-bottom: 20px;
            opacity: 0.7;
        }

        .no-results h3 {
            font-size: 1.4rem;
            margin-bottom: 10px;
            color: var(--pure-white);
        }

        .no-results p {
            color: rgba(255, 255, 255, 0.7);
            max-width: 500px;
            margin: 0 auto 20px;
        }

        .suggestions {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            justify-content: center;
            margin-top: 20px;
        }

        .suggestion-tag {
            padding: 8px 16px;
            background: rgba(210, 4, 45, 0.1);
            color: var(--primary-red);
            border-radius: 20px;
            font-size: 0.9rem;
            cursor: pointer;
            transition: all 0.3s ease;
            border: 1px solid rgba(210, 4, 45, 0.3);
        }

        .suggestion-tag:hover {
            background: var(--primary-red);
            color: var(--pure-white);
        }

        /* Load More Button */
        .load-more {
            text-align: center;
            padding: 30px 0;
        }

        .load-more-btn {
            padding: 12px 32px;
            background: transparent;
            color: var(--primary-red);
            border: 2px solid var(--primary-red);
            border-radius: 25px;
            font-weight: 600;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 10px;
        }

        .load-more-btn:hover {
            background: var(--primary-red);
            color: var(--pure-white);
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(210, 4, 45, 0.3);
        }

        /* ========== READY SECTION ========== */
        .ready-section {
            padding: 50px 4%;
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
            text-align: center;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            margin-bottom: 40px;
        }

        .ready-section h2 {
            font-size: 1.8rem;
            margin-bottom: 15px;
            color: var(--pure-white);
            font-weight: 700;
        }

        .ready-section p {
            color: rgba(255, 255, 255, 0.8);
            max-width: 600px;
            margin: 0 auto 25px;
            font-size: 1rem;
        }

        .ready-cta {
            display: inline-flex;
            align-items: center;
            gap: 12px;
            padding: 14px 32px;
            background: var(--primary-red);
            color: var(--pure-white);
            border-radius: 25px;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.3s ease;
            border: 2px solid var(--primary-red);
        }

        .ready-cta:hover {
            background: transparent;
            color: var(--primary-red);
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(210, 4, 45, 0.3);
        }

        /* ========== MODAL ========== */
        .portfolio-modal {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.98);
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 2000;
            padding: 20px;
        }

        .portfolio-modal.active {
            display: flex;
            animation: modalFadeIn 0.3s ease;
        }

        @keyframes modalFadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .modal-content {
            background: rgba(0, 0, 0, 0.95);
            border-radius: 16px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            max-width: 900px;
            width: 100%;
            padding: 20px;
            position: relative;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.5);
        }

        .modal-close {
            position: absolute;
            top: 20px;
            right: 20px;
            background: var(--primary-red);
            color: white;
            border: none;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            cursor: pointer;
            font-size: 1.2rem;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
            z-index: 10;
            box-shadow: 0 4px 12px rgba(210, 4, 45, 0.4);
        }

        .modal-close:hover {
            transform: rotate(90deg) scale(1.1);
            background: var(--accent-red);
        }

        .modal-video-container {
            position: relative;
            padding-bottom: 56.25%;
            height: 0;
            overflow: hidden;
            border-radius: 12px;
            background: #000;
            margin-bottom: 20px;
        }

        .modal-video-container iframe {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            border: none;
        }

        /* ========== WHATSAPP FLOAT ========== */
        .whatsapp-float {
            position: fixed;
            width: 60px;
            height: 60px;
            bottom: 25px;
            right: 25px;
            z-index: 1000;
            cursor: pointer;
            transition: all 0.3s ease;
            animation: float 3s ease-in-out infinite;
            filter: drop-shadow(0 4px 10px rgba(0, 0, 0, 0.3));
            display: flex;
            align-items: center;
            justify-content: center;
            background: #25D366;
            /* Official WhatsApp Green */
            border-radius: 50%;
            /* Removed padding to let icon fill better or keep if icon is font */
            padding: 0;
        }

        .whatsapp-float i {
            color: white;
            font-size: 2.2rem;
            /* Larger icon */
        }

        @keyframes float {

            0%,
            100% {
                transform: translateY(0);
            }

            50% {
                transform: translateY(-10px);
            }
        }

        .whatsapp-float:hover {
            transform: scale(1.1) rotate(5deg);
            filter: drop-shadow(0 12px 24px rgba(0, 0, 0, 0.4));
        }

        /* ========== RESPONSIVE ========== */
        @media (max-width: 768px) {
            .demo-header {
                padding: 10px 4%;
            }

            .demo-hero h1 {
                font-size: 1.6rem;
            }

            .demo-hero p {
                font-size: 0.9rem;
            }

            .search-section {
                padding: 0 4% 20px;
            }

            .portfolio-section {
                padding: 0 4% 30px;
            }

            .portfolio-grid {
                gap: 15px;
            }

            .media-title {
                font-size: 1.1rem;
            }

            .media-description {
                font-size: 0.85rem;
            }

            .ready-section {
                padding: 40px 4%;
            }

            .ready-section h2 {
                font-size: 1.6rem;
            }

            .whatsapp-float {
                width: 55px;
                height: 55px;
                bottom: 20px;
                right: 20px;
            }
        }

        @media (max-width: 480px) {
            .portfolio-grid {
                grid-template-columns: 1fr;
            }

            .media-info {
                padding: 15px;
            }

            .action-btn {
                padding: 8px 12px;
                font-size: 0.85rem;
            }

            .search-btn {
                padding: 12px 20px;
                min-width: 100px;
            }
        }
    </style>
</head>

<body>
    <!-- ========== HEADER ========== -->
    <header class="demo-header">
        <div class="header-left">
            <a href="index.html" class="back-btn" aria-label="Back to Home">
                <i class="fas fa-arrow-left"></i>
            </a>
            <div class="header-logo">
                <img src="/public/img/APS Advertising Logo (1).png" alt="APS Advertising Logo" class="header-logo-img">
                <div class="header-logo-text">APS Advertising</div>
            </div>
        </div>

        <div class="header-actions">
            <a href="https://wa.me/917355576785" class="action-btn book-btn" target="_blank" style="padding: 8px 16px;">
                <i class="fab fa-whatsapp"></i>
                <span>Chat with Us</span>
            </a>
        </div>
    </header>

    <!-- ========== HERO SECTION ========== -->
    <section class="demo-hero">
        <h1>Our Creative Portfolio</h1>
        <p>Explore professional video and audio advertisements from APS Advertising</p>
    </section>

    <!-- ========== SEARCH SECTION ========== -->
    <section class="search-section">
        <div class="search-container">
            <div class="search-box">
                <input type="text" class="search-input" id="searchInput"
                    placeholder="Search for ads (business, festival, voice, etc.)">
                <button class="search-btn" onclick="performSearch()">
                    <i class="fas fa-search"></i>
                    <span>Search</span>
                </button>
            </div>

            <div class="filters-container">
                <button class="filter-btn active" onclick="filterByCategory('all')">
                    <i class="fas fa-th"></i>
                    <span>All</span>
                </button>
                <button class="filter-btn" onclick="filterByCategory('video')">
                    <i class="fas fa-video"></i>
                    <span>Video</span>
                </button>
                <button class="filter-btn" onclick="filterByCategory('audio')">
                    <i class="fas fa-microphone-alt"></i>
                    <span>Audio</span>
                </button>
                <button class="filter-btn" onclick="filterByCategory('business')">
                    <i class="fas fa-briefcase"></i>
                    <span>Business</span>
                </button>
                <button class="filter-btn" onclick="filterByCategory('festival')">
                    <i class="fas fa-holiday"></i>
                    <span>Festival</span>
                </button>
                <button class="filter-btn" onclick="filterByCategory('education')">
                    <i class="fas fa-graduation-cap"></i>
                    <span>Education</span>
                </button>
                <button class="filter-btn" onclick="filterByCategory('political')">
                    <i class="fas fa-landmark"></i>
                    <span>Political</span>
                </button>
            </div>
        </div>
    </section>

    <!-- ========== PORTFOLIO SECTION ========== -->
    <section class="portfolio-section">
        <div class="section-header">
            <h2>All Advertisements</h2>
            <div class="results-count" id="resultsCount">Loading...</div>
        </div>

        <!-- Smart Results Notice -->
        <div class="smart-notice" id="smartNotice" style="display: none;">
            <div class="notice-icon">
                <i class="fas fa-lightbulb"></i>
            </div>
            <div class="notice-text" id="noticeText">
                Showing relevant results based on your search
            </div>
        </div>

        <!-- Portfolio Grid -->
        <div class="portfolio-grid" id="portfolioGrid">
            <!-- Content loaded by JavaScript -->
        </div>

        <!-- Load More Button -->
        <div class="load-more">
            <button class="load-more-btn" onclick="loadMoreItems()" id="loadMoreBtn" style="display: none;">
                <i class="fas fa-sync-alt"></i>
                <span>Load More</span>
            </button>
        </div>
    </section>

    <!-- ========== READY SECTION ========== -->
    <section class="ready-section">
        <h2>Ready to Create Your Perfect Ad?</h2>
        <p>Let us bring your vision to life with professional audio and video advertisements</p>
        <a href="contact.html" class="ready-cta">
            <i class="fas fa-paper-plane"></i>
            <span>Start Your Project</span>
        </a>
    </section>

    <!-- ========== VIDEO MODAL ========== -->
    <div class="portfolio-modal" id="portfolioModal">
        <div class="modal-content">
            <button class="modal-close" onclick="closeModal()">
                <i class="fas fa-times"></i>
            </button>
            <div class="modal-video-container">
                <iframe id="modalVideo" src="" frameborder="0"
                    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                    allowfullscreen>
                </iframe>
            </div>
        </div>
    </div>

    <!-- ========== WHATSAPP FLOAT ========== -->
    <a href="https://wa.me/917355576785" class="whatsapp-float" target="_blank" aria-label="Chat on WhatsApp">
        <img src="https://upload.wikimedia.org/wikipedia/commons/6/6b/WhatsApp.svg" alt="WhatsApp"
            style="width: 100%; height: 100%;">
    </a>

    <script>
        // Comprehensive Media Database - Expanded to 30+ items
        // Comprehensive Media Database - Real Content
        const allMediaItems = [
            // --- VIDEO CONTENT ---
            {
                id: 1,
                title: "Male Video Demo",
                description: "Promotional video advertisement (Male Voice)",
                type: "video",
                category: "male",
                thumbnail: "img/poster.png",
                videoUrl: "male/video.mp4",
                tags: ["video", "male", "promotion"]
            },
            {
                id: 2,
                title: "Female Video Demo",
                description: "Promotional video advertisement (Female Voice)",
                type: "video",
                category: "female",
                thumbnail: "img/poster.png",
                videoUrl: "female/video.mp4",
                tags: ["video", "female", "promotion"]
            },
